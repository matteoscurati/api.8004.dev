# =============================================================================
# ERC-8004 Multi-Chain Indexer - Environment Configuration
# =============================================================================
#
# IMPORTANT: Copy this file to .env and fill in your values
# Chain-specific configuration (RPC URLs, contracts) is in chains.yaml
#
# =============================================================================

# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/api_8004_dev
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=2
DB_ACQUIRE_TIMEOUT_SECS=30

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Storage Configuration
MAX_EVENTS_IN_MEMORY=10000

# JWT Authentication
# IMPORTANT: Change JWT_SECRET to a secure random value in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION_HOURS=24

# API Authentication
AUTH_USERNAME=admin

# For development: plain password (INSECURE - use AUTH_PASSWORD_HASH in production)
AUTH_PASSWORD=changeme

# For production: use bcrypt hash
# Generate with: cargo run --bin generate_password_hash
# AUTH_PASSWORD_HASH=$2b$12$...

# CORS Configuration (comma-separated domains)
# Use "*" for all origins (NOT recommended for production)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_SECS=60

# =============================================================================
# NOTES:
# =============================================================================
#
# 1. Chain Configuration:
#    - All chain-specific settings (RPC URLs, contract addresses, starting blocks)
#      are now configured in chains.yaml
#    - See chains.yaml for multi-chain configuration
#
# 2. Security:
#    - Always use strong JWT_SECRET in production (32+ characters)
#    - Always use AUTH_PASSWORD_HASH (not plain AUTH_PASSWORD) in production
#    - Set specific CORS_ALLOWED_ORIGINS (not "*") in production
#    - Adjust RATE_LIMIT_REQUESTS based on your needs
#
# 3. Database:
#    - Ensure PostgreSQL is running and the database exists
#    - Run migrations: sqlx migrate run
#
# =============================================================================
